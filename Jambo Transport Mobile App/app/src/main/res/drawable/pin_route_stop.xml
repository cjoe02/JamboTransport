<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- White fill -->
    <solid android:color="@android:color/white" />

    <!-- Heavy blue border -->
    <stroke
        android:width="4dp"
        android:color="@color/marshall_blue" />

    <!-- Size of the circle -->
    <size
        android:width="20dp"
        android:height="20dp" />
</shape>
